version: '3'

services:
        web:
                image: nginxdemos/hello
                environment:
                        VIRTUAL_HOST: '~^hello\..*\.xip\.io'
                ports:
                        - 80

networks:
        proxy:
                external:
                        name: beluga-proxy
